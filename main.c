#include <stdio.h>
#include <stdlib.h>

struct player_s {
    
}

int main(int argc, char* argv) {
    while(combat) {
        printInfo();
        turnPlayer();
        turnEnemy();
    }

    return EXIT_SUCCESS;
}